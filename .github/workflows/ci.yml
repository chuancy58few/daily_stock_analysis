name: CI

on:
  push:
    branches:
      - analysis/a-share-5y-drawdown-20260815
    paths:
      - scripts/probe_sina_qfq_factors.py
      - .github/workflows/ci.yml
  workflow_dispatch:

concurrency:
  group: a-share-sina-qfq-factor-probe-${{ github.ref }}
  cancel-in-progress: true

jobs:
  qfq-factor-probe:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read

    steps:
      - name: Checkout analysis branch
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install requests
        run: pip install 'requests>=2.31.0'

      - name: Probe factor endpoint
        env:
          PYTHONUNBUFFERED: '1'
        run: python scripts/probe_sina_qfq_factors.py

      - name: Show result
        if: always()
        run: cat reports/sina_qfq_factor_probe/probe.json || true

      - name: Upload result
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: sina-qfq-factor-probe-${{ github.run_number }}
          path: reports/sina_qfq_factor_probe/
          if-no-files-found: warn
          retention-days: 7
